I have fixed the bug where captured photos did not match the viewfinder.
Key changes:
1.  Implemented `applyEIS` and `rotateBuffer` helper functions to consolidate image processing.
2.  Updated `renderViewfinder` to:
    *   Apply EIS stabilization to the preview.
    *   Fix skewed rendering in portrait modes by correctly handling source buffer width in `pushImage`.
    *   Dynamically position HUD elements (brackets, pills, indicators) based on LCD rotation.
3.  Updated `captureAndPreview` to:
    *   Rotate the saved JPEG/BMP images to match the device's physical orientation at capture time.
    *   Ensure consistency between the preview display and the saved file.
4.  Added forward declarations to resolve compilation issues caused by early function calls.
5.  Resolved a missing `runHDRFlow` definition by adding a functional stub.

Verified by successful compilation for ESP32-S3.
